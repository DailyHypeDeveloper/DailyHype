"use client";

import Header from "../../(components)/header";

export default function Profile() {

  return (
    <div>
      <Header currentPage="profile"/>
      <div>
        <label>This is user testing.</label>
      </div>
    </div>
  );
}
